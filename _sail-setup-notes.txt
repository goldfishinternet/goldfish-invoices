# WSL2
install php 8.1

# setup sail project
curl -s https://laravel.build/hr-prototype | bash

#
cd hr-prototype

#
alias sail='[ -f sail ] && bash sail || bash vendor/bin/sail'
./vendor/bin/sail up
./vendor/bin/sail down

# background
sail up -d

sail up
sail down

sail artisan *****?
sail php *****?
sail composer

sail node --version
sail npm run dev
sail yarn

composer require laravel/sail --dev

php artisan sail:install
